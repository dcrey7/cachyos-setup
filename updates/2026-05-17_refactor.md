# Cover Switch QML refactor

- Line count: 324 lines in `coverswitch_g22`, 222 lines in `coverswitch_g23` (-102 lines, -31.5%).
- Removed diagnostic `console.log` calls and the now-unused `panelReserve`, `rawScreenWidth`, and `rawScreenHeight` properties.
- Removed stale generation references and updated the header for the current GNOME-styled Plasma 6 cover switch with the panel-visible workaround.
- Collapsed the extra scene wrapper into the fade layer while keeping the opacity behavior that hides the window after fade-out.
- Kept the bounded `PathView` delegate count, cheap thumbnail rendering, existing path positions, rotations, dim backdrop, and fade/cycling timings.
