# Meta+Up maximize and darker Konsole transparency

- Rebound KWin `Meta+Up` from `Window Quick Tile Top` to `Window Maximize`, while preserving the default reset values.
- Increased the transparent Konsole color scheme opacity from `0.7` to `0.85` for darker, more readable terminals.
